import 'package:flutter/material.dart';

class Emp_login_page extends StatelessWidget {
  const Emp_login_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.blue,
        width: 100,
      ),
    );
  }
}
